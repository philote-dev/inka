# pgrep L5.9 — open decisions and info I need from you

Fill in the `Answer:` lines (or just reply in chat). The bracketed default is what I will do if you say nothing. None of this blocks the work already in flight; these only refine it. Add anything I forgot in section G.

---

## A. Content set (P4)

The AI wrote 486 corpus-grounded items. Triage verified them: nothing physics-blocking, the 60-item spot-check was 100% correct, and every flag was a false positive. Landing about 479 on a branch now; final sign-off is at the merge to main.

- **A1.** Trim the 3 near-duplicate pairs (land 479) or keep both of each (land 482)? [trim to 479]
  > Answer:

- **A2.** `card-0228` states the Stark z selection rule as Δl=±1 without also noting Δm=0 (harmless in its m=0 context). Leave it, or add Δm=0? [leave]
  > Answer:

- **A3.** Want to eyeball the 3 hardest items yourself (lunar tidal expansion, stellar pulsation, linear Stark)? All already verified correct. [not needed]
  > Answer:

- **A4.** Seeded content lands "cold": real cards and problems with no fabricated review history, and the dev demo injector is what lights up the scores. Good? [yes, per your earlier choice]
  > Answer:

- **A5. (needs your call, touches a spec rule)** 15 of the KEEP problems landed with no corpus citation (empty `source_ref`). The spec says every generated item must cite a named source or be refused, so shipping them uncited is a gap. Options: (a) I re-ground those 15 against the corpus and keep the ones that get a real citation, drop the rest; (b) drop all 15 now; (c) keep them uncited and accept the gap. [recommend (a)]
  > Answer:

- **A6.** Near-duplicate dedup kept the lower-numbered of each pair (`prob-0001`, `prob-0106`, `card-0061`). Fine, or did you want the other member of a pair? [as is]
  > Answer:

- **A7. (note, not blocking)** There is a pre-existing difficulty-scale mismatch in the AI problem path (`problem_gen.py` stores difficulty as a 0..1 fraction, which clamps to 1.0 in the Performance model, whereas the landed bundle uses the authored 1..5 scale correctly). Want me to fix that AI-path inconsistency as a small follow-up? [yes, as a follow-up]
  > Answer:

---

## B. UI / P1 (context for your other agent)

- **B1.** The "Run the diagnostic" prompt shows only until a user completes it, then hides. Keep a quiet re-run link afterward, or make it vanish entirely? [keep a quiet re-run]
  > Answer:

- **B2.** Manifold tap to focus drill: any preference on behavior (e.g. tapping a topic opens a small chooser for Cards vs Problems on that topic)? And is a clickable-labels fallback acceptable if full 3D hit-testing is too heavy this pass? [chooser; fallback acceptable]
  > Answer:

- **B3.** Defaults: what is your real target exam date (to prefill the Test date), and your preferred default target retention? [leave date empty; retention 0.90]
  > Answer:

---

## C. Exam mode (P3)

- **C1.** Exam attempts currently also feed your everyday Performance and Readiness (a mock improves the signal). Keep that, or keep exam attempts separate? They carry a `mode:exam` tag so we can split them later either way. [keep feeding]
  > Answer:

- **C2.** Exam length: full-length capped by available problems, plus a ~20-question short section. Want a different fixed section size or randomized selection? [as is]
  > Answer:

---

## D. Settings

- **D1.** Export saves a timestamped `.colpkg` to your Downloads folder (no file picker). Fine, or want a chooser? [as is]
  > Answer:

- **D2.** Reset scope is already confirmed: clears progress (attempts + resets sample cards), keeps settings and generated content, two-click confirm. Any change? [no change]
  > Answer:

---

## E. Sync demo (desktop to mobile)

- **E1.** The Settings sync URL defaults to `http://127.0.0.1:8090/`, but `just sync-server` listens on `8080`. Should I align them, and to which port? [align to 8080]
  > Answer:

- **E2.** Want me to write an exact copy-paste step-by-step (plus a one-command helper) for the desktop-to-mobile sync demo? [yes]
  > Answer:

---

## F. Ship / integration

- **F1.** Merge L5.9 into `main` via a PR you review first, or a solo fast-forward once the gate is green? [PR for your review]
  > Answer:

- **F2.** Do you want a live "AI on" in-app demo (installs the AI deps into the app runtime and wires your key), so generation and the tutor run live inside the app? Not needed for the bundled content. [skip for now]
  > Answer:

- **F3.** The E4 deliverables (results report, model cards, Brainlift) are next-layer (L6) work. Want me to start drafting any now? [wait]
  > Answer:

---

## G. Anything else you want changed
