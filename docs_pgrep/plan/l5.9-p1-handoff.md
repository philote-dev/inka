# pgrep L5.9 — P1 UI handoff prompt

Paste the block below into the other agent to continue the UI polish. Start it only after the current P1 pass has committed to `l5.9-p1-ux` (the controller will confirm, with screenshots), so two agents are not editing the same branch at once.

---

You are continuing the UI/UX polish of pgrep, a Physics-GRE study app forked from Anki. The visual work is human-led; the human (Frank) will review your changes.

## Where to work

The git worktree `.worktrees/l5.9-p1-ux` (branch `l5.9-p1-ux`), at repo root `/Users/philote/projects/inka`. Keep `just lint` and `just test-py` green. Do not touch other `.worktrees/*`.

## The stack

- Desktop UI is SvelteKit under `ts/routes/pgrep/`. The nav shell is `ts/routes/pgrep/+layout.svelte` (with `ts/lib/components/NavRail.svelte`). The Home hero manifold is `ts/lib/components/Manifold.svelte` (2D D3 fallback) and `Manifold3D.svelte` (Three.js), backed by `ts/lib/pgrep/manifold.ts` and `manifold3d.ts`. Surfaces call the Python bridge via `pgrepCall` from `ts/routes/pgrep/lib/bridge.ts`.
- Read the design contract `design/ux-foundation.md` in full: the token system and warm-neutral palette, the three reserved score hues (amber Memory, blue Performance, lilac Readiness, used only as a data language), the honesty anatomy of a score card, the nav shell (section 4), the manifold with tap-to-focus-drill (section 5), and the surfaces (section 7).

## Hard rules

- Copy rule: no em-dashes, no colon-heavy phrasing, short labels, honest and lightly human voice.
- Speed rule: no interaction blocks the UI over 100ms.
- Both light and dark themes must be beautiful. Use design tokens only, no raw colors.
- Keep the scores honest: abstain on thin data, never hardcode a number. Never mutate scheduling state. Everything must still work with AI off.

## What the previous agent already did on this branch (verify against the latest commits and its report)

1. Nav rail active-state now follows the current route.
2. The top-left "pgrep" wordmark links to Home.
3. The "Run the diagnostic" prompt is first-run-only (hidden once completed), with a quiet re-run entry.
4. The manifold topics are clickable and launch a topic-scoped focus drill. This was the hardest item; it may have shipped as a fallback (clickable labels) rather than full 3D hit-testing. Check what shipped.
5. The Library surface was reworded so the internal terms "seed", "gap-fill", "stylize" are gone; it reads as "write a flashcard in your own words and we build a set from it".
6. A dev-only Tools menu entry opens the lab gallery.

## Your job

Continue polishing the surfaces (Home, Progress, Diagnostic, Library, Study, Settings) against `ux-foundation.md`, in both themes: refine layout, spacing, rhythm, copy, and the score-card states (default, abstain, AI-off, loading, empty). If the manifold focus-drill interaction (item 4) shipped as a fallback, upgrade it to real region hit-testing that maps a tapped region to its topic. Commit focused changes on this branch. When you finish a batch, hand the branch and a short summary back to Frank for review.

---
