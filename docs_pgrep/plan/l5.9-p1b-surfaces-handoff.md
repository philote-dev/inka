# pgrep L5.9 — P1b UI handoff prompt (Progress, Library, Settings)

Paste the block below into a fresh agent. It runs in its own worktree and never edits the surfaces or shared files that the P1 agent (Home, Study, Diagnostic, Cards, Exam, Math) is holding. The two branches merge back into `l5.9-integration` cleanly because their file sets are disjoint.

Base note. Every L5.9 stream branch (`p2b-settings`, `p3-exam`, `p4-content`, `p5-demo`, `polish`) is already merged into `l5.9-integration`, so that is the up-to-date base. The old stream branches are 25 or more commits behind; do not reuse them.

## Setup (run once)

```
cd /Users/philote/projects/inka
git worktree add .worktrees/l5.9-p1b-surfaces -b l5.9-p1b-surfaces l5.9-integration
cd .worktrees/l5.9-p1b-surfaces
```

Only one Anki app can bind port 40000 at a time. To see your changes live, ask Frank to restart `just run` from this worktree. He runs one app at a time and switches which worktree it launches from.

---

You are continuing the UI/UX polish of pgrep, a Physics-GRE study app forked from Anki. The visual work is human-led and Frank reviews your changes. You are the P1b agent and you own three surfaces: Progress, Library, and Settings. Another agent (P1) owns Home, Study, Diagnostic, Cards, Exam, and all math rendering. Stay in your lane so the two branches merge cleanly.

## Where to work

Worktree `.worktrees/l5.9-p1b-surfaces` (branch `l5.9-p1b-surfaces`), at repo root /Users/philote/projects/inka. This worktree is gitignored, so the Glob and Grep tools return nothing inside it. Use `rg --no-ignore`, `ls`, and Read with explicit absolute paths. Keep `just lint` and `just test-py` green. Do not touch other `.worktrees/*`.

## Your lane (write freely)

- ts/routes/pgrep/progress/**
- ts/routes/pgrep/library/**
- ts/routes/pgrep/settings/**
- ts/lib/components/ReliabilityDiagram.svelte and ts/lib/components/CoverageBar.svelte (Progress-only visualizations)
- pylib/anki/pgrep/settings.py (Settings backend). If you must add a bridge endpoint, coordinate first, because the bridge file is shared.
- New components you create for these surfaces. Name them clearly.

## Do not edit (single-writer, request changes through Frank)

- ts/routes/pgrep/+layout.svelte, ts/routes/pgrep/+page.svelte (Home), study/**, diagnostic/**
- ts/lib/components/: NavRail, ScoreCard, CardFace, ChoiceList, Manifold, Manifold3D, StudyFrame, GradeBar, HintRung
- ts/lib/sass/_pgrep.scss and ts/routes/pgrep/pgrep.scss (shared tokens and styles). Keep surface-specific styles scoped inside your own +page.svelte. If you need a global token or a shared-component change, write the request in your handback note and Frank will have the P1 agent land it.
- ts/lib/pgrep/{math,manifold,manifold3d}.ts
- pylib/anki/pgrep/content_bundle.json

## The stack

Desktop UI is SvelteKit under ts/routes/pgrep/. Surfaces call the Python bridge via `pgrepCall` from ts/routes/pgrep/lib/bridge.ts. Shared components live in ts/lib/components/. Read the design contract at design/ux-foundation.md (try /Users/philote/projects/inka/design/ux-foundation.md and /Users/philote/projects/inka/design/claude-design/docs/pgrep/planning/ux-foundation.md), the token system, the three reserved score hues (amber Memory, blue Performance, lilac Readiness, used only as a data language), and the honesty anatomy of a score card. The claude-design mockups are at /Users/philote/projects/inka/design/claude-design/*.dc.html (Library.dc.html, Settings.dc.html, Diagnostic.dc.html, States.dc.html, Component Library.dc.html). Open decisions relevant to your surfaces are in /Users/philote/projects/inka/docs_pgrep/plan/l5.9-decisions.md, sections D (Settings) and E (Sync demo).

## Hard rules

- Copy rule: no em-dashes, no colon-heavy phrasing, short labels, honest and lightly human voice.
- Speed rule: no interaction blocks the UI over 100ms.
- Both light and dark themes must be beautiful. Use design tokens only, no raw colors.
- Keep the scores honest: abstain on thin data, never hardcode a number. Never mutate scheduling state. Everything must still work with AI off.

## How to work (same loop as the P1 agent)

- Orchestrate. For anything you can parallelize, dispatch read-only audit subagents to produce punch lists, and isolated write subagents that only create new files. Keep shared-file edits single-threaded through yourself.
- Ask Frank. When a decision is his (behavior, scope, wording, defaults), ask him directly with concrete options and a recommendation. He is fielding questions from several agents, so keep each one tight, self-contained, and say which surface it concerns.
- Verify. Build and check with the project `just` recipes. To see changes live, ask Frank to restart `just run` from this worktree.
- Hand back. Commit focused changes on your branch, then give Frank a short summary plus any change requests for the shared files, so he can have the P1 agent land them.

## Your first moves

1. Audit Progress, Library, and Settings against the mockups and ux-foundation.md, in both themes, for layout, spacing, rhythm, copy, and the score-card honesty states (default, abstain, AI-off, loading, empty). Produce a punch list.
2. Confirm the known Settings item: the sync URL default is http://127.0.0.1:8090/ but `just sync-server` listens on 8080. Ask Frank whether to align to 8080.
3. Send Frank the punch list plus the open decisions, then start at the top.

---
