FSRS grade row (Again / Hard / Good / Easy) for the flashcard reveal state.

```jsx
<GradeBar onGrade={(g) => grade(g)} />
```

Four equal monochrome buttons; intervals in mono beneath each label. Never color-code grades. Never flatter or punish.
