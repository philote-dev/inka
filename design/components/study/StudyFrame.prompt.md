Minimal session chrome for any study mode; wraps content in a focused single column.

```jsx
<StudyFrame progress={7} total={20} topic="Mechanics" topicTone="performance" onClose={exit}>
  ...problem or card...
</StudyFrame>
```

Topic tone amber for the cards door, blue for problems. Content leads, chrome recedes.
