Monochrome action button; use for every clickable action, one primary per screen.

```jsx
<Button onClick={start}>Start session</Button>
<Button variant="secondary">Show the step</Button>
```

Variants primary (filled), secondary (outlined), ghost (bare). Sizes md, sm. Never color a button with the score hues or state colors.
